{
  "name": "project-tint",
  "displayName": "Project Tint",
  "description": "Tint the Vscode on startup so multiple windows are easy to tell apart.",
  "author": {
    "name": "Nate"
  },
  "publisher": "nate",
  "icon": "assets/icon.png",
  "galleryBanner": {
    "color": "#373436",
    "theme": "dark"
  },
  "license": "MIT",
  "version": "0.0.1-beta.4",
  "engines": {
    "vscode": "^1.105.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/natezhengbne/vscode-project-tint.git"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "projectTint.apply",
        "title": "Project Tint: Apply (stub)"
      },
      {
        "command": "projectTint.reset",
        "title": "Project Tint: Reset Customizations"
      }
    ],
    "configuration": {
      "title": "Project Tint",
      "properties": {
        "projectTint.autoApply": {
          "type": "boolean",
          "default": true,
          "description": "Automatically apply title bar color on startup without prompting."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "@types/vscode": "^1.105.0",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "@vscode/vsce": "^3.6.2",
    "eslint": "^9.36.0",
    "typescript": "^5.9.3"
  }
}